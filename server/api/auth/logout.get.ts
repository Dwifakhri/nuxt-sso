import { eventHandler } from "h3"

export default eventHandler(async (event) => {
  // await $fetch(`${useRuntimeConfig().public.API_URL}logout`, {
  //   method: "get",
  // }).then((res) => {
  //   console.log(res)
  // })
})
